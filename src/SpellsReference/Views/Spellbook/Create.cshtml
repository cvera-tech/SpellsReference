@model SpellsReference.Models.ViewModels.SpellbookViewModel

@{
    ViewBag.Title = "Create Spell";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>@ViewBag.Title</h2>

@using (Html.BeginForm("Create", "Spellbook", FormMethod.Post, new { id = "CreateSpellbook" }))
{
    @Html.AntiForgeryToken()

    @Html.ValidationSummary()

    <div class="form-group row">
        <div class="col-sm-3">
            @Html.LabelFor(sb => sb.Name)
            @Html.TextBoxFor(sb => sb.Name, new { @class = "form-control" })
        </div>
    </div>

    <div>
        <button id="CreateSpellbookButton" type="submit" class="btn btn-success btn-lg">Submit</button>
        @Html.ActionLink("Cancel", "Index", "Spellbook", null, new { @class = "btn btn-outline-danger btn-lg" })
    </div>
}